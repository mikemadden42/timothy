[project]
name = "timothy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Michael Madden", email = "mikemadden42@icloud.com" }
]
requires-python = ">=3.14"
dependencies = [
    "ollama>=0.6.2",
]

[project.scripts]
timothy = "timothy:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "ruff>=0.16.7",
]
